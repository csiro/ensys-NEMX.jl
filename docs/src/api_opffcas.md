# OPFFCAS API

Full reference for `NEMX.OPFFCAS`, AC/DC optimal power flow with FCAS
co-optimisation. See [OPF with FCAS](@ref) for the workflow.

Only the names this submodule exports appear here. The problem builders,
formulation-specific methods and visualisation helpers are reached as
`NEMX.OPFFCAS.name`; see the source tree described in [Architecture](@ref).

```@docs
NEMX.OPFFCAS
```

```@autodocs
Modules = [NEMX.OPFFCAS]
Order = [:type, :constant, :function, :macro]
```
