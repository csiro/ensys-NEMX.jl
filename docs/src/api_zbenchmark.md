# ZBenchmark API

Full reference for `NEMX.ZBenchmark`, the zonal dispatch reconstruction.
See [Zonal benchmarking](@ref) for the workflow and [Prices and duals](@ref) for
what the outputs mean.

```@docs
NEMX.ZBenchmark
```

```@autodocs
Modules = [NEMX.ZBenchmark]
Order = [:type, :constant, :function, :macro]
```
